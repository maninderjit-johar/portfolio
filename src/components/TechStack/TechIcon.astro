---
import { Image } from "astro:assets";
const { title, url } = Astro.props;
---

<div class="flex flex-col gap-2 items-center">
  <Image src={url} alt={title} width={50} />
  <p class="opacity-90 font-semibold">{title}</p>
</div>
